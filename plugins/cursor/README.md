# taleseal — Cursor

Coming — blocked on the `taleseal` CLI's Cursor session reader. Not yet listed in any
marketplace manifest.

Cursor stores sessions locally in SQLite (`~/.cursor/chats/`); once the CLI can read them,
this directory gains the same preview → confirm → publish flow as the Claude Code and Codex
plugins.

Until then: https://taleseal.com
